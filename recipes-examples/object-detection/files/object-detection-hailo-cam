#!/usr/bin/env python3
"""YOLOv8n object detection on a live camera, via the Hailo-8 AI accelerator."""

import sys

from object_detect.camera_cli import run

sys.exit(run("hailo", require_source=False))
